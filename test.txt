CREATE TABLE nikita (id INTEGER PRIMARY KEY, name VARCHAR(g20)) ON COMMIT DELETE ROWS;
CREATE GLOBAL TEMPORARY TABLE table_name (something CHAR(g50) NOT NULL UNIQUE, reg DATE);
CREATE TABLE name (name REAL) ON COMMIT PRESERVE ROWS;

CREATE TABLE Orders (
    orderID INTEGER NOT NULL,
    OrderNumber INTEGER NOT NULL,
    PersonID INTEGER,
    PRIMARY KEY (34),
    FOREIGN KEY (PersonID) REFERENCES Persons(PersonID),
    UNIQUE ((OrderID OrderNumber))
);
